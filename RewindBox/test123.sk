command /autointeractplots:
	permission: op
	trigger:
		execute player command "rg flag 1 -g members use allow"
		execute player command "rg flag 1 -g members interact allow"
		execute player command "rg flag 2 -g members use allow"
		execute player command "rg flag 2 -g members interact allow"
		execute player command "rg flag 3 -g members use allow"
		execute player command "rg flag 3 -g members interact allow"
		execute player command "rg flag 4 -g members use allow"
		execute player command "rg flag 4 -g members interact allow"
		execute player command "rg flag 5 -g members use allow"
		execute player command "rg flag 5 -g members interact allow"
		execute player command "rg flag 6 -g members use allow"
		execute player command "rg flag 6 -g members interact allow"
		execute player command "rg flag 7 -g members use allow"
		execute player command "rg flag 7 -g members interact allow"
		execute player command "rg flag 8 -g members use allow"
		execute player command "rg flag 8 -g members interact allow"
		execute player command "rg flag 9 -g members use allow"
		execute player command "rg flag 9 -g members interact allow"
		execute player command "rg flag 10 -g members use allow"
		execute player command "rg flag 10 -g members interact allow"
		execute player command "rg flag 11 -g members use allow"
		execute player command "rg flag 11 -g members interact allow"
		execute player command "rg flag 12 -g members use allow"
		execute player command "rg flag 12 -g members interact allow"
		execute player command "rg flag 13 -g members use allow"
		execute player command "rg flag 13 -g members interact allow"
		execute player command "rg flag 14 -g members use allow"
		execute player command "rg flag 14 -g members interact allow"
		execute player command "rg flag 15 -g members use allow"
		execute player command "rg flag 15 -g members interact allow"
		execute player command "rg flag 16 -g members use allow"
		execute player command "rg flag 16 -g members interact allow"
		execute player command "rg flag 17 -g members use allow"
		execute player command "rg flag 17 -g members interact allow"
		execute player command "rg flag 18 -g members use allow"
		execute player command "rg flag 18 -g members interact allow"
		execute player command "rg flag 19 -g members use allow"
		execute player command "rg flag 19 -g members interact allow"
		execute player command "rg flag 20 -g members use allow"
		execute player command "rg flag 20 -g members interact allow"
		execute player command "rg flag 21 -g members use allow"
		execute player command "rg flag 21 -g members interact allow"
		execute player command "rg flag 22 -g members use allow"
		execute player command "rg flag 22 -g members interact allow"
		execute player command "rg flag 23 -g members use allow"
		execute player command "rg flag 23 -g members interact allow"
		execute player command "rg flag 24 -g members use allow"
		execute player command "rg flag 24 -g members interact allow"
		execute player command "rg flag 25 -g members use allow"
		execute player command "rg flag 25 -g members interact allow"
		execute player command "rg flag 26 -g members use allow"
		execute player command "rg flag 26 -g members interact allow"
		execute player command "rg flag 27 -g members use allow"
		execute player command "rg flag 27 -g members interact allow"
		execute player command "rg flag 28 -g members use allow"
		execute player command "rg flag 28 -g members interact allow"
		execute player command "rg flag 29 -g members use allow"
		execute player command "rg flag 29 -g members interact allow"
		execute player command "rg flag 30 -g members use allow"
		execute player command "rg flag 30 -g members interact allow"
		execute player command "rg flag 31 -g members use allow"
		execute player command "rg flag 31 -g members interact allow"
		execute player command "rg flag 32 -g members use allow"
		execute player command "rg flag 32 -g members interact allow"